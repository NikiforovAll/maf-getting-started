---
theme: default
layout: default
---

# What did we cover?

- YAML workflow with `OnConversationStart` trigger
- `InvokeAzureAgent` steps for multi-agent pipelines
- Server-side registration via `CreateAgentVersionAsync`
- Same `RunStreamingAsync` API — Foundry orchestrates

---
layout: section
---

# Next up: evaluations