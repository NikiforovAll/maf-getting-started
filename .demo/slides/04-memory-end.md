---
theme: default
layout: default
---

# What did we cover?

- The default `InMemoryChatHistoryProvider` gives zero-config memory inside one process
- `SerializeSessionAsync()` turns an active session into portable JSON
- `DeserializeSessionAsync()` restores the thread for continued conversation

---
layout: section
---

# Next up: a custom chat history provider