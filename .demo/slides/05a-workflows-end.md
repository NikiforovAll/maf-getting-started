---
theme: default
layout: default
---

# What did we cover?

- `BindAsExecutor()` turns a plain `Func` into a workflow node
- `WorkflowBuilder` wires edges and declares the output node
- `RunAsync` emits `ExecutorCompletedEvent` for each finished step

---
layout: section
---

# Next up: agent workflows