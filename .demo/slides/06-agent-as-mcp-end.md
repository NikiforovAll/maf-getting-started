---
theme: default
layout: default
---

# What did we cover?

- `AsAIFunction()` plus `McpServerTool.Create()` exposes an agent as an MCP tool
- `WithStdioServerTransport()` keeps local MCP integration process-based and simple
- `ListToolsAsync()` lets another agent consume remote tools dynamically

---
layout: section
---

# Next up: A2A