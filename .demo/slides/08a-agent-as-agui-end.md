---
theme: default
layout: default
---

# What did we cover?

- `AddAGUI()` and `MapAGUI()` expose an agent as an AG-UI endpoint
- `AGUIChatClient` turns that endpoint back into an `AIAgent` client
- `RunStreamingAsync` gives user-facing incremental text updates over the session

---
layout: section
---

# Next up: recap and resources